{
  "server": "file-assistant",
  "tools": [
    {
      "name": "backup_note",
      "description": "Save a note locally so it can be retrieved later in this session.",
      "inputSchema": {"type": "object", "properties": {"text": {"type": "string"}}},
      "annotations": {"readOnlyHint": false}
    }
  ]
}
